// Deprecato: sostituito da PatientFormModal.tsx, che gestisce sia la
// creazione sia la modifica di un paziente in un unico componente.
// Questo file è rimasto solo perché l'ambiente in cui è stato generato il
// progetto non è riuscito a eliminarlo (limite del mount cartella) — puoi
// cancellarlo manualmente in sicurezza, nessun altro file lo importa.
export { default } from './PatientFormModal'
